i made an esp32 draw its own signal trace onto an eink display.

every packet it sees becomes a line. after 24h the screen looks like a neural net's fever dream.

the sensor is the medium. the display is the witness. all i did was connect them.
